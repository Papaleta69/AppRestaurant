import 'package:flutter/material.dart';

void main() => runApp(const MyAppDesayunos());

class MyAppDesayunos extends StatelessWidget {
  const MyAppDesayunos({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {

       return const Center(
         child: Text("Jugo", style: TextStyle(fontSize: 40),),
         


         
       );
       
  
  }
}
